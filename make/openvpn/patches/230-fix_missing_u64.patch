--- configure
+++ configure
@@ -6352,7 +6352,7 @@
 # Set -std=c99 unless user already specified a -std=
 case "${CFLAGS}" in
   *-std=*) ;;
-  *)       CFLAGS="${CFLAGS} -std=c99" ;;
+  *)       CFLAGS="${CFLAGS} -std=gnu99" ;;
 esac
 
 #
